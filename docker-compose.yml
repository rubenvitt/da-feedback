services:
  da-feedback:
    image: ghcr.io/rubenvitt/da-feedback:main
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - data:/data
    environment:
      - DAF_BASE_URL=https://feedback.example.com
      - DAF_DB_PATH=/data/feedback.db
      - DAF_OIDC_ISSUER=https://id.example.com
      - DAF_OIDC_CLIENT_ID=da-feedback
      - DAF_OIDC_CLIENT_SECRET=${DAF_OIDC_CLIENT_SECRET}
      - DAF_SENTRY_DSN=${DAF_SENTRY_DSN:-}
      - DAF_SENTRY_ENVIRONMENT=${DAF_SENTRY_ENVIRONMENT:-production}
      - DAF_SENTRY_TRACES_SAMPLE_RATE=${DAF_SENTRY_TRACES_SAMPLE_RATE:-1.0}
      - DAF_SENTRY_RELEASE=${DAF_SENTRY_RELEASE:-}
      - DAF_SENTRY_FRONTEND_LOADER_URL=${DAF_SENTRY_FRONTEND_LOADER_URL:-}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.da-feedback.rule=Host(`feedback.example.com`)"
      - "traefik.http.routers.da-feedback.tls.certresolver=letsencrypt"
      - "traefik.http.services.da-feedback.loadbalancer.server.port=8080"

volumes:
  data:
